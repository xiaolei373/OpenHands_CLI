[build-system]
build-backend = "hatchling.build"
requires = [ "hatchling>=1.25" ]

[tool.uv]
# Limit third-party package intake to versions released at least 7 days ago.
exclude-newer = "7 days"
# First-party packages exempt from 7-day freshness cutoff.
exclude-newer-package = { "openhands-sdk" = false, "openhands-tools" = false }

[project]
name = "openhands"
version = "1.15.1"
description = "OpenHands CLI - Terminal User Interface for OpenHands AI Agent"
readme = "README.md"
license = { text = "MIT" }
authors = [ { name = "OpenHands Team", email = "contact@all-hands.dev" } ]
requires-python = "==3.12.*"
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "openhands-sdk==1.19.1",
  "openhands-tools==1.19.1",
  "openhands-workspace==1.11.1",
  "rich<14.3.0",
  "textual>=8.0.0,<9.0.0",
  "typer>=0.17.4",
  "agent-client-protocol>=0.8.1,<0.9.0",
  "pydantic>=2.7",
  "textual-autocomplete>=4.0.6",
  "pyperclip>=1.9.0",
  "httpx>=0.25.0",
  "textual-serve>=1.1.3",
  "streamingjson>=0.0.2",
]

scripts = { openhands = "openhands_cli.entrypoint:main" }

[dependency-groups]
# Hatchling wheel target: include the package directory
dev = [
  "flake8>=6",
  "gevent>=24.2.1,<26",
  "pre-commit>=4.3",
  "pyinstaller>=6.15",
  "pytest>=8.4.1",
  "pytest-asyncio>=0.21.0",
  "pytest-cov>=5.0.0",
  "pytest-forked>=1.6.0",
  "pytest-xdist>=3.6.1",
  "pytest-textual-snapshot>=1.1.0",
  "pytest-rerunfailures>=14.0",
  "ruff>=0.12.10",
  "pycodestyle>=2.12.0",
  "pyright[nodejs]>=1.1.405",
  "pre-commit>=4.3.0",
  "textual-dev>=1.8.0",
  "pytest-httpserver>=1.1.3",
  "watchfiles>=1.1.1",
  "respx>=0.22.0",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = [ "openhands_cli" ]

# uv source pins for internal packages

# Ruff configuration
[tool.ruff]
target-version = "py312"
line-length = 88
# Vendored public skills (openhands_cli/skills) are third-party content shipped
# as data; do not lint or reformat them. force-exclude makes the exclusion apply
# even when files are passed explicitly (e.g. by pre-commit).
extend-exclude = ["openhands_cli/skills"]
force-exclude = true

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes (includes F841: unused-variable)
    "I",    # isort
    "UP",   # pyupgrade
    "ARG",  # flake8-unused-arguments
]
# Enforce rules that catch mutable defaults and related pitfalls
# - B006: mutable-argument-default
# - B008: function-call-in-default-argument
# - B039: mutable-contextvar-default
# - RUF012: mutable-class-default
extend-select = ["B006", "B008", "B039", "RUF012"]

[tool.ruff.lint.per-file-ignores]
# Test files often have unused arguments (fixtures, mocks, interface implementations)
"tests/**/*.py" = ["ARG"]




[tool.ruff.lint.isort]
known-first-party = ["openhands"]
combine-as-imports = true
force-single-line = false
lines-after-imports = 2

# Pytest configuration
[tool.pytest.ini_options]
testpaths = [
    "tests"
]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
asyncio_mode = "auto"
markers = [
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
]

[tool.coverage.run]
relative_files = true
omit = [ "tests/*", "**/test_*" ]

[tool.coverage.paths]
source = [
  "openhands_cli/",
  "openhands-cli/openhands_cli/",
]

[tool.pyright]
include = [
    "openhands_cli",
    "hooks"
]
exclude = [
    "tests",
    "tui_e2e",
    "build.py",
    "hooks",
    "openhands_cli/skills",
]
extraPaths = [
    "."
]
venvPath = "."
venv = ".venv"
pythonVersion = "3.12"
useLibraryCodeForTypes = true
typeCheckingMode = "standard"

# USE EXACT COMMIT FROM AGENT-SDK (PR #1368 - Add condense method to conversation class)

# [tool.uv.sources]
# openhands-sdk = { git = "https://github.com/OpenHands/software-agent-sdk.git", subdirectory = "openhands-sdk", rev = "c76e5bd220ea9eb95a9d351313bdb4e533c9da6e" }
# openhands-tools = { git = "https://github.com/OpenHands/software-agent-sdk.git", subdirectory = "openhands-tools", rev = "c76e5bd220ea9eb95a9d351313bdb4e533c9da6e" }
